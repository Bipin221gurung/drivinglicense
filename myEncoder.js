// myEncoder.js

const { TextEncoder, TextDecoder } = require('util');

const utf8Encoder = new TextEncoder();

// Export the TextEncoder instance or any other functionality you need
module.exports = utf8Encoder;